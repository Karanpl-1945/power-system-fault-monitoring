# Dataset Notes

The active benchmark dataset is PROTECT-90.

Keep large files local and out of Git:

```text
hv_double_line_90kv_preprocessed_data.zip
hv_double_line_90kv_preprocessed_data/
```

The original dataset README is stored in:

```text
docs/datasets/PROTECT90_README.md
```

